# Konechnie_avtomati

### Определение
**Конечный автомат (FSM)** — это модель вычислений, которая в любой момент времени находится ровно в одном из конечного множества состояний. Переход между состояниями происходит по событиям/условиям.  
  
**Виды:**
* Детерминированные (DFA):
  Для каждого состояния и входного символа существует ровно **один переход** в следующее состояние. Это самый частый вид в программировании.
* Недетерминированные (NFA):
  Для одного и того же входа может быть **несколько путей или переходы** без входного сигнала (ε-переходы). В коде часто преобразуются в DFA для реализации.  
  
-------------
### Ссылка на код:
https://github.com/AlexanderB1578/Konechnie_avtomati/blob/main/konechnie%20avtomati.cpp

-------------
### Задание
Я сделал простой конечный автомат для моделирования телефонного звонка 

Cостояния: ожидание → набор номера → соединение → разговор → завершение + ошибки.  


![avtomat.png](avtomat.png)
